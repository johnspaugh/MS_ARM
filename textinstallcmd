sudo apt install vim

sudo apt install
sudo apt install qemu-system-arm
#sudo apt install qemu-arm #didn't recgonize this cmd
sudo apt install github
sudo apt install xv6-aarch64
sudo apt install openssh-server 

sudo apt-get install gdb
				 				 

//how to leave qemu access, how to quit
//"control+A, then press X"

"man dpkg"
"sudo dpkg --list"
"===+++=="


grep open -r ./
//will search in folder everything that has open in the text				


sudo apt install openssh-server

sudo systemctl status ssh

sudo systemctl restart ssh

#I connect tailscale through my Github account, on ARM device and my PC, 
#so then can see each other's IP connected on the same network
https://login.tailscale.com/admin/welcome

#Given by Professor, to get a key on ARM device then can clone form github, otherwise not authorized to even with pull/push
#https://docs.github.com/en/authentication/connecting-to-github-with-ssh/testing-your-ssh-connection
#https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account
#https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent
#had to use a curl command on another website helped to fix, will need to look up, maybe.

-------------------------------------------------------
#From the Professor,
#HornetSys / raspi3-tutorial, "raspi3-tutorial forked from bztsrc/raspi3-tutorial,"
#github.com, Accessed: Oct. 23, 2025. [Online] Available: 
#https://github.com/HornetSys/raspi3-tutorial/tree/HornetSys-patch-1
#Specific branch used to fine the code for the installs:
#https://github.com/HornetSys/raspi3-tutorial/blob/HornetSys-patch-1/00_crosscompiler/README.md

#Do all the installs from the branch HornetSys-patch-1, path to file, raspi3-tutorial/00_crosscompiler/README.md
#However, when I tried to do the installs, I ran into the error below
johnspaugh@raspberrypi:~/aarch64-elf-toolchain/build-gcc $ $ ls /usr/local/cross-compiler/bin
aarch64-elf-addr2line  aarch64-elf-elfedit    aarch64-elf-gcc-ranlib  aarch64-elf-ld       aarch64-elf-ranlib
aarch64-elf-ar         aarch64-elf-gcc        aarch64-elf-gcov        aarch64-elf-ld.bfd   aarch64-elf-readelf
aarch64-elf-as         aarch64-elf-gcc-7.2.0  aarch64-elf-gcov-dump   aarch64-elf-nm       aarch64-elf-size
aarch64-elf-c++filt    aarch64-elf-gcc-ar     aarch64-elf-gcov-tool   aarch64-elf-objcopy  aarch64-elf-strings
aarch64-elf-cpp        aarch64-elf-gcc-nm     aarch64-elf-gprof       aarch64-elf-objdump  aarch64-elf-strip
bash: $: command not found
aarch64-elf-addr2line: 'a.out': No such file
aarch64-elf-ar: invalid option -- '6'
Usage: aarch64-elf-ar [emulation options] [-]{dmpqrstx}[abcDfilMNoOPsSTuvV] [--plugin <name>] [member-name] [count] archive-file file...
       aarch64-elf-ar -M [<mri-script]
 commands:
  d            - delete file(s) from the archive
  m[ab]        - move file(s) in the archive
  p            - print file(s) found in the archive
  q[f]         - quick append file(s) to the archive
  r[ab][f][u]  - replace existing or insert new file(s) into the archive
  s            - act as ranlib
  t[O][v]      - display contents of the archive
  x[o]         - extract file(s) from the archive
 command specific modifiers:
  [a]          - put file(s) after [member-name]
  [b]          - put file(s) before [member-name] (same as [i])
  [D]          - use zero for timestamps and uids/gids
  [U]          - use actual timestamps and uids/gids (default)
  [N]          - use instance [count] of name
  [f]          - truncate inserted file names
  [P]          - use full path names when matching
  [o]          - preserve original dates
  [O]          - display offsets of files in the archive
  [u]          - only replace files that are newer than current archive contents
 generic modifiers:
  [c]          - do not warn if the library had to be created
  [s]          - create an archive index (cf. ranlib)
  [l <text> ]  - specify the dependencies of this library
  [S]          - do not build a symbol table
  [T]          - deprecated, use --thin instead
  [v]          - be verbose
  [V]          - display the version number
  @<file>      - read options from <file>
  --target=BFDNAME - specify the target object format as BFDNAME
  --output=DIRNAME - specify the output directory for extraction operations
  --record-libdeps=<text> - specify the dependencies of this library
  --thin       - make a thin archive
 optional:
  --plugin <p> - load the specified plugin
 emulation options: 
  No emulation specific options
aarch64-elf-ar: supported targets: elf64-littleaarch64 elf64-bigaarch64 elf32-littleaarch64 elf32-bigaarch64 elf32-littlearm elf32-bigarm pei-aarch64-little elf64-little elf64-big elf32-little elf32-big srec symbolsrec verilog tekhex binary ihex plugin
Assembler messages:
Error: can't open aarch64-elf-gcc-7.2.0 for reading: No such file or directory
aarch64-elf-gcc-7.2.0: Error: can't open aarch64-elf-gcov-dump for reading: No such file or directory
aarch64-elf-gcov-dump: Error: can't open aarch64-elf-nm for reading: No such file or directory
aarch64-elf-nm: Error: can't open aarch64-elf-size for reading: No such file or directory
aarch64-elf-gcc-ar
aarch64-elf-gcov-tool
aarch64-elf-objcopy
aarch64-elf-strings
aarch64-elf-cpp: fatal error: too many input files
compilation terminated.
johnspaugh@raspberrypi:~/aarch64-elf-toolchain/build-gcc $ ls /usr/local/cross-compiler/bin
ls: cannot access '/usr/local/cross-compiler/bin': No such file or directory
-----------------------------------------------------------

#thus, I recieved this error when trying to make on raspi3-tutorial/03_uart1 and 05_uart0
johnspaugh@raspberrypi:~/Documents/CSC290/raspi3-tutorial/03_uart1 $ make
rm kernel8.elf *.o >/dev/null 2>/dev/null || true
aarch64-elf-gcc -Wall -O2 -ffreestanding -nostdinc -nostdlib -nostartfiles -c start.S -o start.o
aarch64-elf-gcc -Wall -O2 -ffreestanding -nostdinc -nostdlib -nostartfiles -c main.c -o main.o
aarch64-elf-gcc -Wall -O2 -ffreestanding -nostdinc -nostdlib -nostartfiles -c uart.c -o uart.o
aarch64-elf-ld -nostdlib -nostartfiles start.o main.o uart.o -T link.ld -o kernel8.elf
aarch64-elf-ld: Error: unable to disambiguate: -nostartfiles (did you mean --nostartfiles ?)
make: *** [Makefile:39: kernel8.img] Error 1

#to fix this error, I got advice to delete the - nostartfiles text in the Makefile
#did git diff to show the changes made in the Makefile, also only hear could change to doulb -, --nostartfiles
git diff
diff --git a/03_uart1/Makefile.gcc b/03_uart1/Makefile.gcc
index 476f415..305026f 100644
--- a/03_uart1/Makefile.gcc
+++ b/03_uart1/Makefile.gcc
@@ -36,7 +36,7 @@ start.o: start.S
        aarch64-elf-gcc $(CFLAGS) -c $< -o $@

 kernel8.img: start.o $(OBJS)
-       aarch64-elf-ld -nostdlib -nostartfiles start.o $(OBJS) -T link.ld -o kernel8.elf
+       aarch64-elf-ld -nostdlib start.o $(OBJS) -T link.ld -o kernel8.elf
        aarch64-elf-objcopy -O binary kernel8.elf kernel8.img

 clean:
diff --git a/03_uart1/kernel8.img b/03_uart1/kernel8.img
index 0eef540..ae125d8 100755
Binary files a/03_uart1/kernel8.img and b/03_uart1/kernel8.img differ
--------------------------------------------------------------------------------

#to setup a remote desktop display of ARM device on pc,
sudo raspbi-config  #special command to open a blue box
3 #select 3 Interface
13 #select vnc
yes #agree to connect
finish # come back to the beginning, so finish to stop.
echo $ DISPLAY #empty blank answer,

#to see the display of ARM device on pc, only need to download tiger vncreview pc and connect by ip or johnspaugh@ip, was still on talcewire guardwire
#connect select 
#!---special command to always remember in cmd----!
export DISPLAY =:0

make run
#now another window in ARM pop up to see output when make run happens, can see it

#Link below has the code to setup DISPLAY to be done at the boot, so don't have to do it, automatically does it, so insure 0.0 is correct what you needed/wanted
https://www.elearningsolutions.co.in/display-environment-variables/ 
#when I tried to do method 1, I would get stuck on the login screen after every power on.
Method 2: Creating a Custom Boot Script
sudo vim /etc/local.rc #if you forget the sudo, the file will not have permission to save.
export DISPLAY=:0.0
:wq #save quit  
Saving the Changes: Press Ctrl+O to save the changes and Ctrl+X to exit the editor.
#after Ctrl+O, will ask if name is good,if good then press enter to confirm

